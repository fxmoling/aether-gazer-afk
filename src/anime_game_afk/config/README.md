# config/

Configuration data models for the automation framework.

## Files

| File | Purpose |
|------|---------|
| `models.py` | GameConfig, TaskResult, and other shared data models |

## Planned Migration

Configuration loading will move to `runtime/config.py` (Wave 1, Layer 3). This directory may be consolidated or kept for backward compatibility.
