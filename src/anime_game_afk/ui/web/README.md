# web/ — Frontend Build Output

Auto-generated by `frontend/` build. **Do not edit files here directly.**

## Files

| File | Purpose |
|------|---------|
| `index.html` | Main HTML page loaded by pywebview |
| `app.js` | Vue 3 application bundle |
| `index.css` | Compiled styles |
| `favicon.svg` | Browser favicon |
| `icons.svg` | Icon sprite sheet |

## Build

```bash
cd frontend && npm run build
```

Output is written here and served by pywebview at runtime.

See `frontend/README.md` for development workflow.
